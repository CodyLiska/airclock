# How to turn this into a fully polished device

- boot splash
- kiosk mode
- systemd service
- watchdog

# How to dramatically improve the UI

- smoother animations
- modern layout
- bigger numbers
- graphs
- animated backgrounds?

# How to turn this into a complete air quality station

- PM2.5
- VOC
- AQI calculations

# Nice product-level upgrades

- save the current screen and restore it after reboot
- persist more settings:
  - snooze minutes
  - selected sound
  - last-used Pomodoro state
- use a better custom alarm WAV instead of the test file

# Hardware / enclosure phase

- build a case
- mount the screen cleanly
- mount the encoder and buttons
- add vent holes for the SCD40
- cable management / strain relief

# UX improvements

- brightness control
- larger custom icons
- color-coded air quality if you later move to a display path that supports it cleanly
- a true home dashboard with status tiles

# Sensor expansion

Since you skipped PM2.5 for now, the obvious future hardware phase would be:

- add PMS5003
- show PM1.0 / PM2.5 / PM10
- combine that with CO₂ for a more complete air-quality screen

# How to turn the CO₂ sensor into a home ventilation analyzer that shows:

- air change rate
- ventilation efficiency
- CO₂ decay curves

# Phase 11 - Nice product-level upgrades

- save the current screen and restore it after reboot
- persist more settings:
  - snooze minutes
  - selected sound
  - last-used Pomodoro state
  - use a better custom alarm WAV instead of the test file

# Phase 12 - UX improvements

- brightness control
- larger custom icons
- color-coded air quality if you later move to a display path that supports it cleanly
- a true home dashboard with status tiles

# Background Ideas

- modern dashboard
- Apple-style widgets
- retro alarm clock
- cyberpunk UI
- make your device look like a tiny retro game console dashboard
